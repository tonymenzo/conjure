"""Conjure tools — orchestral-style tool definitions for the agent API.

Two namespaces:

- `primitives` — spawn, send, recv, wait_for, terminate, introduce, list_inbox.
- `combinators` — LLM-callable wrappers around the Python combinators in
  `conjure.combinators`.
"""
